To install Respect/Template, you can add it as a dependency or by downloading the composer.phar executable.

$ curl -s http://getcomposer.org/installer | php

and run install

$ php composer.phar install

For more details, see http://getcomposer.org.

